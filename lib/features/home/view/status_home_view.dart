import 'package:flutter/material.dart';

class StatusHomeView extends StatelessWidget {
  const StatusHomeView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Status Home View'),
      ),
    );
  }
}
