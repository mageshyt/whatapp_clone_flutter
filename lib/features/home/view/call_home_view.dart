import 'package:flutter/material.dart';

class CallHomeView extends StatelessWidget {
  const CallHomeView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Call Home View'),
      ),
    );
  }
}
